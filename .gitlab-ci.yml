stages:
  - deploy

deploy_production:
  stage: deploy
  image: node:20-alpine
  only:
    - main
  script:
    - npm install -g @railway/cli
    - railway up --service "$RAILWAY_SERVICE_ID" --detach
